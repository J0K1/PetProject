@{
    ViewData["Title"] = "Вход";
}

<h1>Вход</h1>
<form id="login-form">
    <label>Login: <input name="login" /></label><br />
    <label>Password: <input name="password" type="password" /></label><br />
    <button type="submit">Войти</button>
</form>

<script>
    document.getElementById('login-form').addEventListener('submit', e => {
        e.preventDefault();
        const { login, password } = Object.fromEntries(new FormData(e.target));
        // Здесь можно хранить токен/сессию, пока просто редиректим
        window.location.href = `/Pages/Profile?nick=${login}`;
    });
</script>
