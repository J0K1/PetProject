{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "GameDbContext": "Host=localhost;Port=5432;Database=db;Username=postgres;Password=123", // docker = postgres / localhost
    "UserDbContext": "Host=localhost;Port=5432;Database=db;Username=postgres;Password=123", // docker = postgres / localhost
    "Redis": "localhost:6379,abortConnect=false" // docker = redis / localhost
  },
  "Steam": {
    "ApiKey": "E1FF4AE7E3C4CDCA7899F0835BFDC3CE"
  },
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://localhost:5000"
      },
      "Https": {
        "Url": "https://localhost:5001"
      }
    }
  }
}